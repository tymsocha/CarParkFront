import { ElectricityConsumptionForFloor } from './electricity-consumption-for-floor';

describe('ElectricityConsumptionForFloor', () => {
  it('should create an instance', () => {
    expect(new ElectricityConsumptionForFloor()).toBeTruthy();
  });
});
