import { OccupationTimeAmount } from './occupation-time-amount';

describe('OccupationTimeAmount', () => {
  it('should create an instance', () => {
    expect(new OccupationTimeAmount()).toBeTruthy();
  });
});
