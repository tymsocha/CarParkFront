import { ElectricityConsumptionForCarPark } from './electricity-consumption-for-car-park';

// Plik z testami jednostkowymi automatycznie generowany przez Angular CLI
// Każdy komponent i plik o rozszerzeniu .ts posiada odpowiadający mu plik z testami jednostkowymi
describe('ElectricityConsumptionForCarPark', () => {
  it('should create an instance', () => {
    expect(new ElectricityConsumptionForCarPark()).toBeTruthy();
  });
});
